#!/usr/bin/env sh
# Hook commit-msg: decide bump de versión basado en el mensaje.
# Si no se detecta bump semántico, forzamos uno (por defecto patch) para asegurar nueva versión.
# Personaliza exportando DEFAULT_BUMP=minor o major antes del commit si lo deseas.

# node scripts/bump-version.mjs "$1" --force ${DEFAULT_BUMP:-patch}
